define(['backbone'], function(Backbone) {

    return Backbone.View.extend({
        initialize: function() {
            // Do stuff with this.options.collection
        },

        render: function() {
            // Show this.options.collection
        },
    });
});
